---
name: framecast-security-review-2026-03-20
description: Full codebase security review — 2 medium, 4 low findings. PinGate 6-digit bug escalated, media route unauthenticated with quarantine bypass.
type: project
---

## Review Date: 2026-03-20
## Scope: Full codebase (all Python, shell, JSX, systemd, pi-gen)

### Findings: 2 MEDIUM, 4 LOW, 1 INFO

1. MEDIUM — web_upload.py:662-666 — `/media/<path:filename>` unauthenticated, serves quarantine/ subdirectory
2. MEDIUM — PinGate.jsx:101,163 — Hardcoded 4-digit max in handleInput and button disabled check breaks 6-digit PIN support (escalated from LOW)
3. LOW — framecast-health.service — Runs as root with no ProtectSystem/NoNewPrivileges hardening
4. LOW — framecast-kiosk.service — No sandboxing (ProtectSystem, NoNewPrivileges) on WebKit browser process
5. LOW — api.py:147,310 — schedule_days setting passes through with no server-side validation (latent injection when shell script consumes it)
6. LOW — updater.py:128 — _atomic_write uses predictable `.tmp` suffix instead of mkstemp (inconsistent with config.py pattern)
7. INFO — auth.py:187 — Previous stale docstring confirmed fixed

### Previous Findings Status
- Map.jsx XSS: FIXED (textContent)
- auth.py 0000 docstring: FIXED
- PinGate.jsx 4-digit: STILL PRESENT, escalated to MEDIUM
- DisplayRouter.jsx 0000: FIXED

### Clean Categories
- SQL injection (all parameterized ?)
- Command injection (all subprocess list-form, no shell=True)
- Path traversal on delete/upload/thumbnail (resolve+relative_to, secure_filename)
- Secrets in git (.env gitignored, .env.example empty)
- Unsafe deserialization (no yaml/eval)
- Weak cryptography (SHA-256 throughout, secrets module for PINs)
- HMAC validation (compare_digest, fail-closed ephemeral key)
- SameSite=Strict + httponly cookies
- Origin header validation on state-changing methods
- Rate limiting (adaptive PIN + API-wide)
- CSP headers present
- Systemd main service hardened
- UFW firewall (RFC1918 only)
- OTA update SHA256 + HMAC rollback
- File upload limits + decompression bomb protection
- XSS (Preact auto-escape, textContent in map popup)
