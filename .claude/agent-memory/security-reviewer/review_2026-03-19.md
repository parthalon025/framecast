---
name: framecast-security-review-2026-03-19
description: Full security review after 13 PRs — 2 medium, 2 low findings. Previous fixes verified.
type: project
---

## Review Date: 2026-03-19
## Scope: Full codebase (git diff a9bd166..HEAD)

### Findings: 2 MEDIUM, 2 LOW
1. MEDIUM — Map.jsx:73-74 — Leaflet bindPopup XSS via unsanitized filename
2. MEDIUM — auth.py:187 — Stale docstring references "0000" bypass that no longer exists in code
3. LOW — PinGate.jsx:54 — Hardcoded 4-digit PIN length check (should respect PIN_LENGTH config)
4. LOW — DisplayRouter.jsx:27 — Client-side "0000" PIN suppression (cosmetic, stale reference)

### Previous Fixes Verified In Place
- HMAC fallback: fail-closed (ephemeral key, line 83-86)
- secure_filename on thumbnails (line 687)
- 0000 PIN bypass removed from _pin_is_open_access (line 121-123: only empty = open)
- SameSite=Strict on auth cookie (line 272)
- Rate limiting on PIN verification
- Origin header validation on state-changing requests

### Clean Categories
- SQL injection (all 44+ queries use parameterized ?)
- Command injection (all subprocess calls use list args, no shell=True)
- Path traversal on delete (resolve + relative_to check)
- Secrets in git (app/.env gitignored, .env.example has no values)
- No unsafe deserialization found
- hashlib.md5/sha1 for security — none (SHA-256 throughout)
- Hardcoded credentials — none
