{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "state:current",
  "description": "Sent on initial SSE connect with the current server state. The 'clients' field may be absent if state retrieval fails.",
  "type": "object",
  "properties": {
    "connected": {
      "type": "boolean",
      "const": true
    },
    "clients": {
      "type": "integer",
      "minimum": 0
    }
  },
  "required": ["connected"],
  "additionalProperties": false
}
